[phases.setup]
nixPkgs = ["php", "phpExtensions.zip"]

[phases.build]
cmds = [
  "composer install --no-interaction --optimize-autoloader",
  "php artisan config:cache",
  "php artisan route:cache"
]

[start]
cmd = "php artisan serve --host=0.0.0.0 --port=$PORT"
